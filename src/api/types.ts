export type Series = {
  title: string;
  url: string;
  image: string;
  genres: string[];
};

export type Episode = {
  number: number;
  title: string;
  size: string;
  url: string;
  season: string;
};
